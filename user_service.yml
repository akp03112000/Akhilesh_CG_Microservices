spring:
  application:
    name: user_service

  datasource:
    url: jdbc:mysql://localhost:3306/userservices_db
    username: root
    password: 0311

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 7077

management:
  endpoints:
    web:
      exposure:
        include: "*"
